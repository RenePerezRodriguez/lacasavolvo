/**
 * Identificador de build inyectado por Vite (`define`). Se compara en runtime
 * contra `version.json` del servidor para detectar una versión nueva y forzar recarga.
 */
declare const __APP_VERSION__: string;
